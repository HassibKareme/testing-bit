import React, { ReactNode } from 'react';
import type { AccordionProps } from '../../../api/accordion/accordion';

export function Accordion({ elementList }: AccordionProps) {
  return (
    <div>Hello world from react Accordion</div>
  );
}
